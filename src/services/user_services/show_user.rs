/*use diesel::prelude::*;

use crate::database::{init_db_pool::{DbPool, establish_connection},schema::users::dsl::*, User};
use actix_web::{
    web::Data,
    Result,
};
use diesel::QueryDsl;*/

fn find_user_by_id(user_id: i32) {
    /*let connection = &mut establish_connection();
    let result = users
        .filter(id.eq(user_id))
        .limit(1)
        .load::<User>(&mut );
*/
}
